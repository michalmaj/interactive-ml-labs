[project]
name = "ml-lab-core"
version = "0.1.0"
description = "Shared core utilities for Interactive ML Labs."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"